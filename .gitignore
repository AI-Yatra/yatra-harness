.venv/
.runs/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.env
.DS_Store
.ruff_cache/

# REPL-generated per-message task files. Generated on the fly; not part of
# the workshop material. Delete the whole folder to start fresh.
tasks/chat/

# Local scratch: probe configs, adversarial scenarios, API check scripts.
.probe/

# Session findings kept alongside the code but deliberately untracked.
docs/FINDINGS.md
docs/FINDINGS-CAPABILITY-AUDIT.md

# Local tool state (Command Code writes personal preference notes here).
.commandcode/
.evals/

# The atlas site is built from source; its dependencies are not vendored.
node_modules/

# Conversation history written by the ay REPL into the working directory.
.ay/
